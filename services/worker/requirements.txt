# Worker: the repair agent and everything it needs to build and test a clone.
httpx>=0.27
packaging>=24.0
google-cloud-firestore>=2.17
# The worker consumes the queue the scanner fans out to. Absent until the
# receiving half of that existed, which is why it was missing: the import is
# inside the function that needs it, so nothing failed until a container
# actually reached that line in the cloud.
google-cloud-pubsub>=2.23
google-adk>=2.0
google-genai>=1.0
PyGithub>=2.4
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-gcp-trace>=1.7
